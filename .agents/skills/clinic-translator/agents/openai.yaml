interface:
  display_name: "Clinic Translator"
  short_description: "Repo-specific patterns and workflows for clinic-translator"
  default_prompt: "Use the clinic-translator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true